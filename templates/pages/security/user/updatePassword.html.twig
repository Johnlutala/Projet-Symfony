{% extends 'base.html.twig' %}
{% block title %}Update-User
{% endblock %}

{% block body %}

	
{% for message in app.flashes('success') %}
	<div class="alert-warning  fade show mb-2 flash-message">
		 <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer">{{ message }}</button>
	</div>

{% endfor %}


	<class="form-container">
    </br>
	{# read and display just one flash message type #}
{# {% for message in app.flashes('warning') %}
	<div class="alert-warning">
		{{ message }}
	</div>

{% endfor %} #}
		{# <h3 id="af">Bienvenue sur notre Formulaire Ingédient</h3> #}

		{{ form_start(form) }}
		{# templates/recettes/form.html.twig #}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="card shadow">
                <div class="card-header bg-dark text-white">
                    <h3 class="mb-0">Formulaire de la Modification de Mot de Pass Utilisateur</h3>
					
                </div>
                <div class="card-body p-4">
                    {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}
                    
                    <div class="row">
                        {{ form_row(form.plainPassword) }}
                 
                    </div>
                    
                    <div class="row">
						{{ form_row(form.newPassword) }}
                        {{ form_row(form.submit) }}
                    </div>
                    
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>

		{{ form_end(form) }}
		</div>
</div>
{% endblock %}
